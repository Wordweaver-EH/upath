/**
 * P_NEG1_1 Variable Identification Step Configuration
 * Exactly matches the working prototype's step config
 */

import { StepId } from '../../../types';
import { StepConfig } from '../../core/interfaces';

/**
 * Step configuration for P_NEG1_1_VARIABLE_IDENTIFICATION
 * Exactly matches the working prototype configuration
 */
export const stepConfig: StepConfig = {
  id: StepId.P_NEG1_1_VARIABLE_IDENTIFICATION,
  title: "P-1.1: Variable Identification",
  part: "PartNeg1",
  isJsonOutput: true,
};