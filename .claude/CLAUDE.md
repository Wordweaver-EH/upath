# .claude/ Directory

Contains Claude Code session config for this project. Primary project context is in the root `CLAUDE.md`.

## Memory

Session memories are stored in `memory/` (auto-managed by Claude Code across conversations).
